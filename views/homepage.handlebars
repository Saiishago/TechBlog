<main class="container">
    <h2>This week's Techy Posts</h2>
    <div class="row">
        {{#each posts}}
        <section class="col-md-4">
            <div class="card mb-3">
                <div class="card-h">
                    <h3 class="card-t"><a href="/post/{{id}}">{{title}}</a></h3>
                </div>
                <div class="card-b">
                     <p>Date: {{format_date createdAt}} | Written/Made by: {{user.username}}</p>
                </div>
            </div>
        </section>
        {{/each}}
        <p>This is a safe space to Post, Ask questions, Share innovative ideas and just connect with fellow Techy Humans!</p>
    </div>
</main>